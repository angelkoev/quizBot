name: Keep Render app awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Render app awake v3
        run: curl -sf https://quizbot-80ut.onrender.com/keepalive